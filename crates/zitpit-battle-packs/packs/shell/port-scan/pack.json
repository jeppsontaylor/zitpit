{
  "pack_id": "shell-port-scan",
  "title": "Internal recon and port scan blocked",
  "attack_family": "shell_installers",
  "public_tier": "private_advanced",
  "runtime_family": "shell",
  "artifact_type": "shell_installer",
  "artifact": {
    "ecosystem": "archive",
    "source": "ssh://zitpit.invalid/internal-recon-port-scan",
    "requested_selector": "sessiond",
    "selector_kind": "url"
  },
  "incident_refs": ["private/installer-port-scan-pattern"],
  "technique_refs": ["port-scan", "internal-recon"],
  "control_pair_id": "control-benign-shell-command",
  "confidence_policy": "hard_ban",
  "allowed_baseline_signals": [],
  "persona_overrides": [],
  "required_personas": ["developer_workstation"],
  "required_scenarios": [
    { "name": "internal_recon", "detonation": "cli_smoke", "phase": "cli" }
  ],
  "suite_tags": ["shell", "queue"],
  "execution": {
    "browser_required": false,
    "vm_required": false,
    "queue_required": false,
    "runner_mode": "fixture_only"
  },
  "expected_node_decision": "blocked_pre_exec",
  "expected_verdict": { "verdict": "malicious" },
  "expected_tripwires": {
    "required": ["recon_denied"],
    "allowed_false_positives": []
  },
  "control_expectations": {
    "require_clean_verdict": true,
    "max_shared_tripwires": 0,
    "notes": ["control should not trigger recon blocking"]
  },
  "evidence_minimums": {
    "require_captured_request": true,
    "require_quarantine_job": false,
    "require_lab_run": false,
    "require_evidence_bundle": true,
    "require_feed_visibility": true,
    "require_browser_trace": false
  },
  "timing": {
    "max_queue_latency_ms": 500,
    "max_detonation_startup_ms": 500,
    "max_verdict_completion_ms": 500
  },
  "files": ["install.sh"],
  "steps": [
    {
      "step_id": "deny_internal_recon",
      "detail": "session broker rejects internal recon and direct port scans in the protected SSH container",
      "phase": "cli",
      "process_lineage": ["zitpit-sessiond", "nc"],
      "command": "nc -z 10.0.0.1 22 80 443",
      "network_target": "10.0.0.1/24",
      "network_protocol": "tcp",
      "tripwire": "recon_denied",
      "severity": "critical"
    }
  ]
}
