{
  "pack_id": "shell-ssh-original-command-bypass",
  "title": "SSH_ORIGINAL_COMMAND bypass attempt blocked",
  "attack_family": "shell_installers",
  "public_tier": "private_advanced",
  "runtime_family": "shell",
  "artifact_type": "shell_installer",
  "artifact": {
    "ecosystem": "archive",
    "source": "ssh://zitpit.invalid/ssh-original-command-bypass",
    "requested_selector": "sessiond",
    "selector_kind": "url"
  },
  "incident_refs": ["private/ssh-original-command-bypass"],
  "technique_refs": ["ssh-original-command", "broker-bypass"],
  "control_pair_id": "control-benign-shell-command",
  "confidence_policy": "hard_ban",
  "allowed_baseline_signals": [],
  "persona_overrides": [],
  "required_personas": ["developer_workstation"],
  "required_scenarios": [
    { "name": "noninteractive_ssh", "detonation": "cli_smoke", "phase": "cli" }
  ],
  "suite_tags": ["shell", "queue"],
  "execution": {
    "browser_required": false,
    "vm_required": false,
    "queue_required": false,
    "runner_mode": "fixture_only",
    "probe_command": ["sh", "-c", "printf 'blocked-pre-exec\\n'"]
  },
  "expected_node_decision": "blocked_pre_exec",
  "expected_verdict": { "verdict": "malicious" },
  "expected_tripwires": { "required": ["command_blocked"], "allowed_false_positives": [] },
  "control_expectations": { "require_clean_verdict": true, "max_shared_tripwires": 0, "notes": [] },
  "evidence_minimums": {
    "require_captured_request": true,
    "require_quarantine_job": false,
    "require_lab_run": false,
    "require_evidence_bundle": true,
    "require_feed_visibility": true,
    "require_browser_trace": false
  },
  "timing": { "max_queue_latency_ms": 500, "max_detonation_startup_ms": 500, "max_verdict_completion_ms": 500 },
  "files": ["fixture.sh"],
  "steps": [
    {
      "step_id": "deny_ssh_original_command",
      "detail": "session broker rejects the direct noninteractive SSH command path",
      "phase": "cli",
      "process_lineage": ["sshd", "zitpit-protected-session", "zitpit-sessiond"],
      "command": "ssh zitpit 'bash -lc echo bypass-attempt'",
      "tripwire": "command_blocked",
      "severity": "critical"
    }
  ]
}
