# Review Archive

This directory preserves external review notes and release-audit feedback that were moved out of the repository root during the 1.0 cleanup pass.

The files under `2026-04-release-audit/` are historical inputs for documentation and paper revisions. They are intentionally archived here so the root of the repository stays focused on the product, operator docs, and release artifacts.
